[pytest]
addopts =
    -v
    --tb=short
    --strict-markers
    -p no:randomly
    --timeout=120

timeout = 120

testpaths = .

markers =
    integration: full end-to-end integration tests (require live services)
