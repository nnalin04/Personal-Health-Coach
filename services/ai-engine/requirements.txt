fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.2
python-multipart==0.0.12
google-generativeai==0.8.3
google-cloud-storage==2.18.2
pypdf==5.1.0
pytest==8.3.3
pytest-cov==6.0.0
httpx==0.27.2
aio-pika==9.4.3
langchain==0.3.7
langchain-google-genai==2.0.5
pgvector==0.3.5
psycopg2-binary==2.9.10
# ── Multi-provider AI clients ──────────────────────────────────────────────────
groq>=0.11.0
anthropic>=0.40.0
# ── PaddleOCR (local OCR pre-pass for medical reports, cuts LLM token cost ~80%) ─
# ARM64 guard: PaddleOCR has Linux arm64 wheels (Oracle VM) but not macOS arm64.
# The ImportError is handled gracefully in paddleocr_client.py — service runs fine without it.
paddlepaddle==2.6.2; sys_platform != "darwin" and platform_machine != "arm64"
paddleocr>=2.9.1; sys_platform != "darwin" and platform_machine != "arm64"
